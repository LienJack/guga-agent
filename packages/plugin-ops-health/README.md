# @guga-agent/plugin-ops-health

First-party operations helper for provider health and redacted credential/config views.

The package keeps checks injectable and hermetic by default. It never performs provider network calls unless a host supplies an explicit health checker.
