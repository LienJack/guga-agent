# @guga-agent/host-sdk

Typed client for the local Guga host protocol.

The SDK is intentionally handwritten for this first slice. It depends on shared protocol DTOs and can later be replaced or supplemented by generated clients.
