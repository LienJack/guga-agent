# @guga-agent/host-protocol

Shared host protocol contracts for CLI, local server, SDK, and future Web/desktop viewers.

This package intentionally contains no runtime, HTTP server, provider, tool, or UI implementation. It defines serializable resource DTOs, canonical host events, and SSE envelope helpers.
