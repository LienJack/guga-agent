# @guga-agent/host-local-server

Node HTTP and SSE adapter over `@guga-agent/host-runtime`.

This package owns transport concerns only. Protocol DTOs live in `@guga-agent/host-protocol`, and agent execution remains in `@guga-agent/core` through `@guga-agent/host-runtime`.
