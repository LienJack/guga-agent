# @guga-agent/cli

Command-line entry point for running Guga through the shared host protocol.

```bash
guga run "summarize the repo" --provider openai --model gpt-5
guga run "hello" --mock --debug-events
```
